# Supabase Credentials
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# NOTE: never put secrets (API tokens, service keys) in this file.
# Any VITE_* variable is inlined into the public JS bundle at build time and is
# readable by every visitor. Only public values belong here.

# WhatsApp Contact Number
VITE_WHATSAPP_NUMBER=917007679485
